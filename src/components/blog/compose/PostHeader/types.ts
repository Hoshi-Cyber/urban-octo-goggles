export type PostHeaderProps = {
  title: string;     // required H1
  eyebrow?: string;  // optional label/category
  dek?: string;      // optional short summary under title
  id?: string;       // optional anchor target
  class?: string;    // optional pass-through class hook
};
